@model Projet2_EasyFid.ViewModels.CraListViewModel
@{
    Layout = "_Layout";
    ViewBag.Title = "ManagerAccueil";
}

<h2>Liste des CRA des collaborateurs</h2>

<table id="cras">
    <tr>
        <th>Date création</th>
        <th>Date modification</th>
        <th>Statut</th>
        <th>Nom</th>
        <th>Prénom</th>
        <th>Actions</th>
    </tr>
    @foreach (var cra in @Model.Cras)
    {
        <tr>
            
            <td>@cra.CreatedAt.ToString("dd/MM/yy")</td>
            <td>@cra.UpdatedAt.ToString("dd/MM/yy")</td>
            <td>@cra.StateCra</td>
            <td>@cra.User.UserData.Lastname</td>
            <td>@cra.User.UserData.Firstname</td>
            <td>Détails</td>
            
        </tr>
    }
</table>